let resultado = 5 + (3 * 2); // resultado es 11, la multiplicacion se evalua antes que la suma
console.log(resultado); 

//el orden el cual se evaluan las operaciones en una expresion se determina por la precedencia de los operadores, que es un conjunto de reglas que establecen el orden en el cual se deben evaluar los operadores en una expresion.

//La precedencia de los operadores en JavaScript es la siguiente:
//1. Parentesis ()
//2. Exponenciacion (**)
//3. Multiplicacion (*), Division (/), Modulo (%)
//4. Suma (+), Resta (-)
//5. Operadores de comparacion (==, !=, ===, !==, >, <, >=, <=)
//6. Operadores logicos (&&, ||, !)
//7. Operadores de asignacion (=, +=, -=, *=, /=, %=, **=)
