// las constantes son valores que no pueden ser reasignados, es decir, una vez que se les asigna un valor, no se puede cambiar. Se utilizan para almacenar valores que deben permanecer constantes a lo largo del programa.

const prueba  = "Este valor no puede cambiarse";

console.log(prueba ); // Output: Este valor no puede cambiarse